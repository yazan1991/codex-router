# CLIProxy GPT 6 Astra

## Evidence

The exact checked-in route is `cliproxy/gpt-6-astra`. The route uses the
CLIProxy Responses provider and preserves its namespaced identity through the
router-managed child agent definition and encrypted collaboration relay.
